module.exports = {
  express: {
    port: {{express.port}},
    url: '{{express.url}}'
  },
  mongo: {
    port: {{mongo.port}},
    url: '{{mongo.url}}'
  },
  web: {
    port: {{webpack.port}}
  }
}